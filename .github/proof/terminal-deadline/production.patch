diff --git a/src/gateway/server-methods/terminal.ts b/src/gateway/server-methods/terminal.ts
index 726a5053c2575..8172c9fe06d41 100644
--- a/src/gateway/server-methods/terminal.ts
+++ b/src/gateway/server-methods/terminal.ts
@@ -411,7 +411,7 @@ export async function openTerminalSession(
           context.resolveTerminalLaunchPolicy(refreshedLaunch.plan.agentId).ok &&
           authorizeCatalogTerminalNode(context, relay.plan).ok &&
           !deadline.controller.signal.aborted &&
-          Date.now() < deadline.expiresAtMs,
+          performance.now() < deadline.expiresAtMs,
         command: relay.plan.command,
         params: relay.params,
       });
diff --git a/src/gateway/terminal/open-deadline.ts b/src/gateway/terminal/open-deadline.ts
index f928fbad41a1f..14b0b331eb1d9 100644
--- a/src/gateway/terminal/open-deadline.ts
+++ b/src/gateway/terminal/open-deadline.ts
@@ -10,13 +10,14 @@ export class TerminalOpenDeadlineError extends Error {
 }
 
 type TerminalOpenDeadline = {
+  // Monotonic milliseconds; wall-clock changes must not alter the open budget.
   expiresAtMs: number;
   controller: AbortController;
 };
 
 export function createTerminalOpenDeadline(): TerminalOpenDeadline {
   return {
-    expiresAtMs: Date.now() + TERMINAL_OPEN_DEADLINE_MS,
+    expiresAtMs: performance.now() + TERMINAL_OPEN_DEADLINE_MS,
     controller: new AbortController(),
   };
 }
@@ -32,7 +33,7 @@ export async function waitForTerminalOpenDeadline<T>(
   run: () => Promise<T>,
   deadline: TerminalOpenDeadline,
 ): Promise<T> {
-  if (deadline.controller.signal.aborted || Date.now() >= deadline.expiresAtMs) {
+  if (deadline.controller.signal.aborted || performance.now() >= deadline.expiresAtMs) {
     throw expireTerminalOpenDeadline(deadline);
   }
   return await new Promise<T>((resolve, reject) => {
@@ -42,14 +43,14 @@ export async function waitForTerminalOpenDeadline<T>(
     };
     const timer = setTimeout(
       () => expireTerminalOpenDeadline(deadline),
-      Math.max(0, deadline.expiresAtMs - Date.now()),
+      Math.max(0, deadline.expiresAtMs - performance.now()),
     );
     deadline.controller.signal.addEventListener("abort", onAbort, { once: true });
     let promise: Promise<T>;
     try {
       promise = run();
     } catch (error) {
-      if (deadline.controller.signal.aborted || Date.now() >= deadline.expiresAtMs) {
+      if (deadline.controller.signal.aborted || performance.now() >= deadline.expiresAtMs) {
         expireTerminalOpenDeadline(deadline);
         return;
       }
@@ -60,7 +61,7 @@ export async function waitForTerminalOpenDeadline<T>(
     }
     void promise.then(
       (value) => {
-        if (deadline.controller.signal.aborted || Date.now() >= deadline.expiresAtMs) {
+        if (deadline.controller.signal.aborted || performance.now() >= deadline.expiresAtMs) {
           expireTerminalOpenDeadline(deadline);
           return;
         }
@@ -69,7 +70,7 @@ export async function waitForTerminalOpenDeadline<T>(
         resolve(value);
       },
       (error: unknown) => {
-        if (deadline.controller.signal.aborted || Date.now() >= deadline.expiresAtMs) {
+        if (deadline.controller.signal.aborted || performance.now() >= deadline.expiresAtMs) {
           expireTerminalOpenDeadline(deadline);
           return;
         }
